name: Keep Alive

on:
  workflow_dispatch:

  schedule:
    - cron: '*/5 * * * *'

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping Render
        run: |
          curl -L "https://mohit-portfolio-e21r.onrender.com/health"